<%= content_for :header do %>
    <div class="panel panel-default">
      <div class="panel-heading">
        Search / Create Patient
      </div>
      <div class="panel-body">
        <div class="row">
          <%= simple_form_for :art_patient_search, url: create_from_search_art_patients_path do |f| %>
              <div class="col-sm-9">
                <table width="100%">
                  <tr>
                    <th>
                      <%= f.input :art_number, label: false, placeholder: 'ART #' %>
                      <%= f.input :MRN, label: false, placeholder: 'MRN' %>
                      <%= f.input :gender, collection: ['Male', 'Female'], label: false, prompt: 'Gender' %>
                    </th>
                    <th>
                      <%= f.input :first_name, label: false, placeholder: 'First Name' %>
                      <%= f.input :father_name, label: false, placeholder: 'Father Name' %>
                      <%= f.input :grand_father_name, label: false, placeholder: 'Grand Father Name' %>
                    </th>
                    <th>
                      <%= f.input :phone_number, label: false, placeholder: 'Phone Number' %>
                      <%= f.input :age, as: :string, label: false, placeholder: 'Age' %>
                    </th>
                  </tr>
                  <tr>

                  </tr>
                </table>
              </div>
              <div class="col-sm-3">
                <div class="form-actions">
                  <%= f.button :submit, 'Register New Patient', class: 'btn btn-primary btn-raised' %>
                </div>
              </div>
          <% end %>
        </div>
        <hr/>
        <div id="art_patients_list">
          <%= render 'art_patients' %>
        </div>
      </div>
    </div>
<% end %>